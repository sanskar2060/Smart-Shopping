import React from 'react';
import Navbar from './Navbar';

const Header = () => {
  return (
    <header>
      <Navbar />
      {/* You can add other header elements here like banner, search bar etc. */}
    </header>
  );
};

export default Header;